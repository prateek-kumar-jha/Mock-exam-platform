name: CI Pipeline
on: [push]

jobs:
  build:
    runs-on: windows-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    - name: Run linter
      run: npm install && npm run lint
    - name: Placeholder test step
      run: echo "Placeholder for testing" || true